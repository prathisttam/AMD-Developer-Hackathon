"""Decompose the codebase into analysis tasks."""

from typing import List, Dict


def plan_analysis(codebase_path: str) -> List[Dict[str, str]]:
    """Create analysis tasks from the input codebase."""
    return []
